# an immutable list is called a tuple
# tuples looks just lke list excepy you use parenthesis
dimensions = (200, 50)
print(dimensions[0])

# looping over all values in a tuple using a for loop
new_dimensions = (9, 59, 67)
for dimension in new_dimensions:
    print(dimension)
